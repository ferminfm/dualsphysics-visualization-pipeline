# Deferred scientific roadmap

No item below is authorized by Task 06; each requires a new reviewed runbook.

1. **Internal-nozzle cadence repair.** Make post-restore field scheduling
   explicitly match reference times, then rerun only a bounded exact-time
   comparison. Preserve the uninterrupted reference as the current pressure
   source of record.
2. **Internal-nozzle continuation gate.** Only after cadence repair passes,
   decide whether one longer L7 window or one L8 comparison is justified. Do
   not launch gravity or a parameter matrix by default.
3. **Quarter numerical remedy.** Treat the tested acceleration route as closed.
   Any revival needs a new hypothesis explaining the exit-flow and near-exit
   geometry mismatch, plus a fresh matched full-domain gate before refinement.
4. **Circular/rectangular evidence refresh.** If pressure is needed for those
   routes, instrument new runtime exports; do not infer pressure from existing
   zero-range restored fields.
5. **Model fitting and publication.** Require an explicitly approved model-fit
   protocol, convergence study, uncertainty treatment, and human claim review.
