# Field Panel Correction V3.1

Pressure panels were removed from the field visualization reels. The restored pressure field evidence from Task 05 has zero range, so pressure visualization remains blocked. The V3.1 field panels show only real available fields: phase indicator, velocity magnitude, diagnostic vorticity magnitude, and ambient-phase speed/context. No Q or lambda2 panel is fabricated.

The media remain internal-review proxies only. `fit_ready=false` and `public_ready=false`.
