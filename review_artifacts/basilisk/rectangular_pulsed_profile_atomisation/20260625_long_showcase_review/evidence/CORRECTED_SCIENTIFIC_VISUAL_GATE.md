# Corrected Scientific Visual Gate

Decision: `internal_review_only`

The long official circular control and selected rectangular comparison are corrected as a pulsed-profile/top-hat imposed-inlet benchmark. The selected rectangular route is `C1_rect_area_top_hat` with profile `rect_area_top_hat`; it is not a Poiseuille-selected route and it does not resolve internal nozzle flow.

The boundary audit found no meaningful downstream or lateral boundary contact through `t=2.0` for the official circular and rectangular top-hat routes. The safe hero frame for the primary flythrough is official-round frame 98 at `t=1.96`.

Allowed claim: internal benchmark visualization and morphology review evidence.

Disallowed claims: validation, production CFD, stationary spray, experimental agreement, true atomisation, pressure-atomized-nozzle validation, final prediction, fit-readiness, or public-readiness.
